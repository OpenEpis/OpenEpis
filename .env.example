# Server port (default: 3001)
PORT=3001

DATABASE_URL=postgres://user:password@localhost:5432/openepis

# 32-byte hex key for encrypting API keys at rest. Generate with: openssl rand -hex 32
ENCRYPTION_KEY=
